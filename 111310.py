def evaluate(s):
    s = input()
    s1 = "".join(i for i in s if i.isdigit())  # добавляем в строку s все элементы, которые являются числами,
    # а знаки заменяем пробелами
    s2 = "".join(j for j in s if j.isdigit() == 0)  # добавляем в строку s все элементы, которые являются числами,
    # а знаки заменяем пробелами
    s3 = int(s1)
    print(s)


evaluate(s=str)
def evaluate(s):
    s = input()
    s1 = "".join(i for i in s if i.isdigit())  # добавляем в строку s все элементы, которые являются числами,
    # а знаки заменяем пробелами
    s2 = "".join(j for j in s if j.isdigit() == 0)  # добавляем в строку s все элементы, которые являются числами,
    # а знаки заменяем пробелами
    s3 = int(s1)
    print(s)


evaluate(s=str)
